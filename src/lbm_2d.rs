/*
pub struct LBM2D {
    width: usize,
    height: usize,
    q: usize,
    lattice_velocities_input: Vec<wgpu::Buffer>,
    lattice_velocities_output: Vec<wgpu::Buffer>,
}

pub impl LBM2D {
    pub fn new() {

    }
}
*/
